# Database Configuration
DB_HOST=localhost
DB_NAME=commercio_db
DB_USERNAME=root
DB_PASSWORD=
DB_PORT=3306

# Application Configuration
APP_ENV=development
APP_DEBUG=true
APP_URL=http://mockerjson.xyz

# Security (generate a secure key for production)
APP_KEY=your-secret-app-key-here

# SSL Configuration (for production)
SECURE_SSL=false